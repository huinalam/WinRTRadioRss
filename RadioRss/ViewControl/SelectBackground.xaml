<UserControl
    x:Class="RadioRss.ViewControl.SelectBackground"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RadioRss.ViewControl"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="600"
    d:DesignWidth="250">
	<UserControl.Resources>
		<DataTemplate x:Key="BG_Image_DataTemplate">
			<Grid Width="400" Height="100">
                <Image HorizontalAlignment="Left" Height="100" VerticalAlignment="Top" Width="250" Source="{Binding BGImageSource}" Stretch="Fill" />
                <TextBlock TextWrapping="Wrap" VerticalAlignment="Bottom" Height="30" Width="250" FontSize="20" HorizontalAlignment="Left" TextAlignment="Right" Text="{Binding Name}"/>
                
			</Grid>
		</DataTemplate>
	</UserControl.Resources>
    
    <Grid>
        <Viewbox Height="600" Width="400">
            <ScrollViewer Height="600" Width="400">
                <ListView x:Name="LV_Background"  Height="600" Width="400" ItemTemplate="{StaticResource BG_Image_DataTemplate}" SelectionChanged="LV_Background_SelectionChanged">
                    
                </ListView>
            </ScrollViewer>
        </Viewbox>
    </Grid>
</UserControl>
